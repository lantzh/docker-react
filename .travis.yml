language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t lantzh/docker-react -f Dockerfile.dev .
script:
    - docker run -e CI=true lantzh/docker-react npm run test -- --coverage
